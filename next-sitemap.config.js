/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://harislukman.my.id', // Ganti dengan URL situs Anda
    generateRobotsTxt: true, // (Opsional)
}

module.exports = config
